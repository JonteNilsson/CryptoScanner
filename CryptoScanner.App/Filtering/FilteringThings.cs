namespace CryptoScanner.App.Filtering
{
    public class FilteringThings
    {
        // sortera i stigande bokstavsordning efter namnet på valutan
        //  string orderedCurrencyName = name.OrderBy(n => n.Name);

        //sortera i fallande bokstavsordning efter namnet på valutan
        //  string orderedCurrencyName = name.OrderByDecending(n => n.Name);


        // List<int> numbers = new List<int> { 1, 14, 12, 1100, 99 }

        // var orderedNumbers = numbers.OrderBy(n => n).ToList(); // Sorterar och konverterar till lista

    }
}
